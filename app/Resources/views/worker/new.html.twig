{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Ajouter un ouvrier</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Ajouter" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('worker_index') }}">Retour à la ligne</a>
        </li>
    </ul>
{% endblock %}
