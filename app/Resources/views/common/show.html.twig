{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Commun</h1>

    <table>
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ common.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('common_index') }}">Retour à la liste</a>
        </li>
        <li>
            <a href="{{ path('common_edit', { 'id': common.id }) }}">Modifier</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Supprimer">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
