{% extends 'base.html.twig' %}

{% block body %}
    <h1>Common</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ common.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ common.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('common_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('common_edit', { 'id': common.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
