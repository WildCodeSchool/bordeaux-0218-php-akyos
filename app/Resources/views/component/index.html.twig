{% extends 'layout.html.twig' %}

{% block body %}
    <h1>Listes des équipements</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Catégorie</th>
                <th>Type</th>
                <th>Photo</th>
                <th>Référence</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for component in components %}
            <tr>
                <td>{{ component.category }}</td>
                <td>{{ component.type }}</td>
                <td>{{ component.photo }}</td>
                <td>{{ component.reference }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('component_show', { 'id': component.id }) }}">Voir</a>
                        </li>
                        <li>
                            <a href="{{ path('component_edit', { 'id': component.id }) }}">Modifier</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('component_new') }}">Ajouter des équipements</a>
        </li>
    </ul>
{% endblock %}
