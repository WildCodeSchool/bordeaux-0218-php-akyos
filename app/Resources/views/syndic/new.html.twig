{% extends 'base.html.twig' %}

{% block body %}
    <h1>Syndic creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-primary btn-block" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('syndic_index') }}" class="btn btn-primary">Back to the list</a>
        </li>
    </ul>
{% endblock %}
