<!-- Header -->
<header id="header-navbar" class="hidden-print content-mini content-mini-full">
    <!-- Header Navigation Right -->
    <ul class="nav-header pull-right">
        <li>
            {% if not is_granted('ROLE_ADMIN') %}
                <a class="btn btn-danger intervention" href="{{ path('intervention_new') }}">
                    <i class="fa fa-flash"></i>
                    Demande d'intervention
                </a>
            {% endif %}
        </li>
        <li>
            <div class="btn-group">
                <button class="btn btn-default btn-image dropdown-toggle" data-toggle="dropdown" type="button">
                    <img src="{{ asset('img/avatars/avatar10.jpg') }}" alt="Avatar">
                    <span class="caret"></span>
                </button>
                   <ul class="dropdown-menu dropdown-menu-right">
                    <li class="dropdown-header">Profil</li>
                       <li>
                           <a tabindex="-1" href="{{ path('user_show', {'id':app.user.id}) }}">
                               <i class="si si-user pull-right"></i>
                               Profile
                           </a>
                       </li>
                    <li>
                        <a tabindex="-1" href={{ path ("fos_user_security_logout") }}>
                            <i class="si si-logout pull-right"></i>Déconnexion
                        </a>
                    </li>
                    </ul>
            </div>
        </li>
    </ul>
    <!-- END Header Navigation Right -->

    <!-- Header Navigation Left -->
    <ul class="nav-header pull-left">
        <li class="hidden-md hidden-lg">
            <!-- Layout API, functionality initialized in App() -> uiLayoutApi() -->
            <button class="btn btn-default" data-toggle="layout" data-action="sidebar_toggle" type="button">
                <i class="fa fa-navicon"></i>
            </button>
        </li>
        <li class="hidden-xs hidden-sm">
            <!-- Layout API, functionality initialized in App() -> uiLayoutApi() -->
            <button class="btn btn-default" data-toggle="layout" data-action="sidebar_mini_toggle" type="button">
                <i class="fa fa-ellipsis-v"></i>
            </button>
        </li>
    </ul>
    <!-- END Header Navigation Left -->
</header>
<!-- END Header -->