<!-- Sidebar -->
<nav id="sidebar">
    <!-- Sidebar Scroll Container -->
    <div id="sidebar-scroll">
        <!-- Sidebar Content -->
        <!-- Adding .sidebar-mini-hide to an element will hide it when the sidebar is in mini mode -->
        <div class="sidebar-content">
            <!-- Side Header -->

            <div class="side-header side-content bg-white-op">
                <a href="{{ path('dashboard') }}">
                    <img src="{{ asset('img/dms-dijon.png') }}" class="img-responsive">
                </a>
            </div>
        </div>
        <!-- END Side Header -->
        <!-- Side Content DMS -->
        <div class="side-content side-content-full">
            <ul class="nav-main">
                <li>
                    <a class="nav-submenu" data-toggle="nav-submenu" href="#">
                        <i class="fa fa-file-text-o"></i>
                        {% if  is_granted('ROLE_ADMIN') %}
                            Interventions
                        {% else %}
                            Mes demandes d'intervention
                        {% endif %}
                    </a>
                    <ul>
                        {% if  is_granted('ROLE_ADMIN') %}
                        <li>
                            <a href="{{ path("intervention_new") }}">Créer</a>
                        </li>
                        <li>
                            <a href="{{ path("intervention_index", {'progress' : 'a-planifier'}) }}">À planifier</a>
                        </li>
                        {% endif %}
                        <li>
                            <a href="{{ path("intervention_index", {'progress' : 'en-cours'}) }}">En cours</a>
                        </li>
                        <li>
                            <a href="{{ path("intervention_index", {'progress' : 'a-venir'}) }}">À venir</a>
                        </li>
                        <li>
                            <a href="{{ path("intervention_index", {'progress' : 'realisees'}) }}">Archivées</a>
                        </li>
                    </ul>
                </li>
                {% if  is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="nav-submenu" data-toggle="nav-submenu" href="#"><i class="si si-wrench">
                        </i>Intervenants</a>
                    <ul>
                        <li>
                            <a href="{{ path('worker_index') }}">Liste des intervenants</a>
                        </li>
                        <li>
                            <a href="{{ path('worker_new') }}">Créer nouvel intervenant</a>
                        </li>
                    </ul>
                </li>
                <li>
                    <a class="nav-submenu" data-toggle="nav-submenu" href="{{ path('condominium_index') }}"><i
                                class="fa fa-building">
                        </i>Syndicats</a>
                    <ul>
                        <li>
                            <a href="{{ path('syndicate_index') }}">Liste de tous les syndicats</a>
                        </li>
                        <li>
                            <a href="{{ path('syndicate_new') }}">Créer un nouveau syndicat</a>
                        </li>
                    </ul>
                </li>
                {% endif %}
                <li>
                    <a class="nav-submenu" data-toggle="nav-submenu" href="{{ path('condominium_index') }}">
                        <i class="fa fa-building"></i>
                            Copropriétés
                    </a>
                    <ul>
                        <li>
                            <a href="{{ path('condominium_index') }}">Liste des copropriétés</a>
                        </li>
                        <li>
                            <a href="{{ path('condominium_new') }}">Créer une nouvelle copropriété</a>
                        </li>
                    </ul>
                <li>
                <li>
                    <a class="nav-submenu" data-toggle="nav-submenu" href="{{ path('condominium_index') }}"><i
                                class="si si-user-follow">
                        </i>Utilisateurs</a>
                    <ul>
                        <li>
                            <a href="{{ path('user_index') }}">Voir les utilisateurs</a>

                        </li>
                    </ul>
                    <ul>
                        {% if is_granted('ROLE_ADMIN') %}
                            <li>
                                <a href="{{ path('user_new') }}">Ajouter un utilisateur</a>

                            </li>
                        {% endif %}
                    </ul>

                </li>
                {% if not is_granted('ROLE_ADMIN') %}
                    <li>
                        <a href="{{ path('contact_index') }}"><i class="fa fa-envelope-o"></i>Nous contacter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
        <!-- END Side Content DMS -->
    </div>
    <!-- END Sidebar Scroll Container -->
</nav>
<!-- END Sidebar -->
